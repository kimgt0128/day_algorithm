#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

// players_len은 배열 players의 길이입니다.
int solution(int players[], size_t players_len, int m, int k) {
    int answer = 0;
    return answer;
}